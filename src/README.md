# Usage prepro.py

```
usage: DOCX to XML Parse using ParlaMint structure 
                                                   
required arguments:

-i INPUT, --input_esp INPUT 
			Input path (DOCX Files)

-o OUTPUT, --output OUTPUT
                        Output path (XML Files)

optional arguments:
  -h, --help            show help message and exit

```

